/**
 * Shared CRM UI — re-exports Harbor patterns for back-compat.
 * Prefer importing from `@/components/patterns` in new code.
 */
export {
  EmptyState,
  PageHeader,
  TemperatureBadge,
} from "@/components/patterns"
